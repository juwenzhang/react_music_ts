import React, { Fragment, memo } from 'react';

const AppFooter: React.FC = () => {
  return (
    <Fragment>
      <footer className="app-footer">hello world, Github-Juwenzhang</footer>
    </Fragment>
  );
};

export default memo(AppFooter);
