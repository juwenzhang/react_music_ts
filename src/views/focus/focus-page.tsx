import React, { Fragment, memo } from 'react';

const FocusPage: React.FC = () => {
  return (
    <Fragment>
      <div>
        <h1>FocusPage</h1>
      </div>
    </Fragment>
  );
};

export default memo(FocusPage);
