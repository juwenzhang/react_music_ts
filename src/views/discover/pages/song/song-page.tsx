import React, { Fragment, memo } from 'react';

const songPage: React.FC = () => {
  return (
    <Fragment>
      <div>
        <div>songPage</div>
      </div>
    </Fragment>
  );
};

export default memo(songPage);
