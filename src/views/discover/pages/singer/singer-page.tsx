import React, { Fragment, memo } from 'react';

const singerPage: React.FC = () => {
  return (
    <Fragment>
      <div>
        <div>singerPage</div>
      </div>
    </Fragment>
  );
};

export default memo(singerPage);
